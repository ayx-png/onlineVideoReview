后台：
    1. 接受客户端的请求（评审项目名）
    2. 判断用户是否有权限创建项目
        -若无权限
            通过消息模型对象返回结果（设置状态、提示信息"无权限"、回显数据）
                 将消息模型对象设置到request作用域中
                 请求跳转到用户中心页面,userSpace.jsp
                 return
    3. 判断项目名是否已存在
        -若已存在：
            通过消息模型对象返回结果（设置状态、提示信息、回显数据）
                 将消息模型对象设置到request作用域中
                 请求跳转到用户中心页面,userSpace.jsp
                 return
    4. 在数据库中添加数据
        -将项目名，主持人信息保存至项目表单
        -user表添加项目id

    5. 重定向到用户中心页面
        -将项目信息展现在主持人的用户中心，提示"主持人，可创建会议"
